[package]
name = "jetdb-cli"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[[bin]]
name = "jetdb"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
jetdb = { path = "../jetdb" }
